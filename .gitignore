# build output
bin/
obj/
*.user
**/out/
publish/
build/
dist/

# clips / audio scratch — never commit media (but keep bundled app assets)
*.mp4
*.wav
!**/Assets/**/*.wav

# images: ignore stray/generated PNGs, but KEEP app + website assets
*.png
!**/Assets/**/*.png
!site/**/*.png

# stray design scratch images at repo root (e.g. "ChatGPT Image ....png")
/*.png

node_modules/
server/data/
.env
