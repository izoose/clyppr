{
  "name": "clipper-share-server",
  "version": "1.0.0",
  "description": "Zero-dependency share server for Clipper (upload clips, watch page, range streaming).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true
}
