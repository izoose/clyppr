<Window x:Class="Clipper.App.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Clipper.App"
        Title="Clyppr" SizeToContent="Height" Width="380" ResizeMode="NoResize"
        WindowStyle="None" WindowStartupLocation="CenterOwner" Background="{StaticResource Bg}"
        FontFamily="{StaticResource AppFont}" TextOptions.TextRenderingMode="Grayscale">
    <Border BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="20">
        <StackPanel>
            <TextBlock x:Name="PromptText" Text="Name" Foreground="{StaticResource Text}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBox x:Name="Input" Style="{StaticResource SearchBox}" KeyDown="Input_KeyDown" AutomationProperties.AutomationId="InputBox"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Style="{StaticResource BarButton}" Content="Cancel" Click="Cancel_Click"/>
                <Button Style="{StaticResource AccentButton}" Content="OK" Padding="20,7" Margin="8,0,0,0" Click="Ok_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
