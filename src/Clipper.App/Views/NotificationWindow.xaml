<Window x:Class="Clipper.App.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize" ShowActivated="False"
        SizeToContent="WidthAndHeight" FontFamily="{StaticResource AppFont}"
        TextOptions.TextRenderingMode="Grayscale">
    <Border Background="#1C1C22" CornerRadius="14" Padding="16,12" BorderBrush="#2E2E38" BorderThickness="1" Margin="16">
        <Border.Effect><DropShadowEffect BlurRadius="24" ShadowDepth="3" Opacity="0.55" Color="#000000"/></Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Image Source="pack://application:,,,/Assets/clipper.png" Width="22" Height="22" VerticalAlignment="Center"/>
            <TextBlock x:Name="Message" Text="Saved the last 1m" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,12,0"/>
            <TextBlock Text="&#xE8C6;" FontFamily="Segoe MDL2 Assets" FontSize="15" Foreground="{StaticResource Good}" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
